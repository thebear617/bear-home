# 熊窝 personal 更新日志

## v0.33.0 — 2026-09-23

feat(todo-board): 视图改成三个——看板（当下）· 甘特图（计划）· 复盘（过去）

- feat: 视图切换加第三个 tab「复盘」，内容 = 过去一年的完成热力图 + 已完成任务清单（两个都是只读的历史视角，放在一起看）
- feat: 删掉 header 里单独的「📜 查看历史」折叠按钮 —— 热力图原来会叠在当前视图上方（在看板里就是热力图压在卡片列上），现在只活在「复盘」tab 里
- feat: 甘特图工具栏去掉「显示已完成复盘」勾选框 —— 已完成清单不再需要手动开关，进「复盘」就能看；`ganttItems()` 依旧只输出进行中
- chore: 状态清理：`currentView` 增加 `'review'`、删除 `historyOpen` / `showDoneReview`，`[data-tb-view]` 支持三个取值；`.todo-board-history-toggle` 样式与 480px 下的适配一并删除
- chore: 热力图区块标题由「📜 已完成历史」改成「📜 完成热力图」（避免与下方「已完成 · 复盘」清单重名，也让这块一眼看出是图）

feat(todo-board): 已完成任务不再上甘特图，改为按周铺开的「复盘清单」

- feat: 甘特图只画在办任务（进行中 + 未开始），`ganttItems()` 不再输出已完成——甘特图回答的是「接下来怎么排」（相对位置、并行、这周排不排得下），已结束的事挂在同一根时间轴上会像还占着日程，而且历史无限增长会把图越撑越长（勾选后行数从 17 回到 5、页面高度 ~1275px 回到 ~375px）
- feat: 工具栏原「显示已完成」勾选改为「显示已完成复盘」，勾选后在甘特图**下方**铺出清单（按天 / 按小时两个精度都有，甘特图本身不变）
- feat: 复盘清单按**周**分组（周一起算），组头写周区间 + `完成 N 项 · 共 N 天`（天数按有排期的条目累加），本周带「本周」标；组内按完成日倒序，完成日取 `completedAt` → 末阶段结束日 → `plannedEnd` → 目标日期
- feat: 每条一行：完成日（悬停出完整日期与「N 天前」）＋ 看板图标 ＋ 标题 ＋ 右侧 `8/29—9/18 · 21 天`；有阶段时补一条**按阶段时长成比例**的缩略条（有整点端点按小时算，否则按天算）＋ 一行阶段名，悬停出每段的起止与耗时（复用甘特图那套悬浮提示）
- feat: 复盘清单**分页**（`DONE_REVIEW_PAGE_SIZE = 20`）：以「周」为最小分页单位、不把一周切成两页，所以每页条数在目标值附近浮动（当前数据 51 条 / 9 个周 → 3 页，16 / 17 / 18 条一页）；分页器沿用看板那套样式与交互（上一页 / `1 / 3` / 下一页），页码越界时自动夹回合法页
- feat: 列表头写「共 N 项 · 按完成日倒序 · 第 1—16 条」，翻页后仍能对上总量；每次重新勾选从第 1 页开始
- feat: 无排期的已完成条目（现在有 9 条在甘特图里根本看不见）也能进清单，只显示完成日 + 标题
- chore: 甘特图图例「已完成」改成「已完成阶段」——已完成任务已不上轴，图例里说的是任务内已完成的阶段
- chore: 状态变量 `ganttShowCompleted` → `showDoneReview`（+ 新增 `doneReviewAll`），勾选框 `data-tb-gantt-completed` → `data-tb-done-review`
- 说明：纯展示层改动，**不动阶段粒度与排期精度、不改数据、不改校验规则**；新增 `.todo-review-*` 一套样式，配色沿用甘特图 / 看板卡片色板

feat(todo-board): 甘特图长文本显示优化

- feat: 阶段名不再按字数硬截断（删掉 `truncatePhaseTitle` / `PHASE_TITLE_MAX = 4`），改成交给 CSS 按**实际可用宽度**省略——5 天的段能显示十几个字（原来明明有 200px 也只给 4 个字，导致「PPT：先实验-再方法-…」与「PPT：按照意见进行修改」都被截成同一个 `PPT：…`，根本分不出是哪一段）
- feat: 很窄的段（典型是 1 天）不再硬塞半截字，改成只显示**阶段序号**（`.todo-gantt-phase-index`，与左侧进度点、阶段弹窗里的编号一一对应）；宽窄切换用 **CSS 容器查询**（`container-type: inline-size` + `@container (min-width: 48px)`），不需要 JS 测量
- feat: 左侧标签列的任务标题改为**最多两行**（`-webkit-line-clamp: 2`），220px 宽度基本能读全；甘特条在行内垂直居中（`.todo-gantt-track { align-items: center }`），行变高不会把条拉粗
- feat: 阶段序号改为**常显**（原先宽段会把它藏起来），宽段的显示变成「序号 - 阶段名」：数字与名字之间用短破折 + 间距隔开（不隔开会粘成 `2骨架搭建`，看着奇怪）；序号常显也方便和左侧进度点、阶段弹窗里的编号对照。嫌破折重的话，换成纯空格或中点是一行 CSS
- chore: 合并两处重复的 `.todo-gantt-phase-copy` 样式

feat(todo-board): 「需求名称」与「具体需求」联动

- feat: 具体需求为空（或还停在自动生成的骨架上）时，按「需求名称」里的 `｜` 分段自动铺 `① 需求词：` 骨架，冒号后面留给用户补细节；用户一旦自己动过具体需求（`data-note-from-title` 标记被移除）就不再自动重写，避免吃掉正在写的内容
- feat: 具体需求标签行右侧新增「＋ 按名称补全」按钮（仅编程看板）：把名称里有、具体需求里还没写的需求**增量追加**成新行（编号接着已有条数往下排）。覆盖判定放宽到「互相包含」——名称写 `知识库分页`、具体需求里已写 `知识库：增加分页逻辑` 时视为已覆盖，不会补出重复行
- feat: **绝不按名称裁剪具体需求**——标题只放 2–4 个最重的项，具体需求是完整清单、允许是标题的超集（`c24` 就是名称 4 项 / 备注 5 行）
- feat: **反向回写**：在「具体需求」里改**光标所在行**冒号前的短词时，需求名称里**位置对应**的那个词跟着更新（第 N 行 ↔ 第 N 个需求词），标题预览同步刷新——标题词数由用户控制，不会被自动加长，也不会整条覆盖人工挑过的短词
- feat: 保存时把行首的**无序列表符号**（`- A` / `* A` / `• A` / `· A`）剥掉再统一编号成 ①②③——随手写成列表也能一键变编号列表；`needShortWord()` 复用同一套剥离规则（同时去掉 `①` / `1.` 编号与冒号后的细节）
- feat: 拆分兼容全角 `｜` 与半角 `|`，沿用 `formatNoteLines` 的 20 条编号上限；只在「编程」看板生效，生活 / 科研的「任务名称 + 备注」不联动
- chore: 新增 `.todo-new-note-sync` 幽灵按钮样式（复用 `.todo-new-field-label` 既有的 flex 布局，`margin-left: auto` 靠右）

feat(todo-board): 「目标日期」改成意向日——可选，且不再与创建日重复显示

- feat: 「目标日期」由必填改为可选（类型里 `date?: string` 本来就是可选的，是弹窗写了 `required`）：标签改「目标日期（可选）」，新增任务默认留空，留空也能保存
- feat: 卡片把目标日期当「打算什么时候做」而不是截止日：**有排期的任务不再显示 📅**——`⏱` 排期才是时间承诺，原来会并排出现「📅 2026-07-19 … 🕒 2026-07-19」两条重复日期（同一任务 `date` 与 `createdAt` 相同时尤其刺眼）；没排期的待办显示中性的「📅 计划 9/25」；留空则整块不显示
- feat: `relativeTime()` 超过一个月（或跨年）不再直接吐原始 ISO 串，改成「7月19日」/「2025年7月19日」，卡片 ✅ / 🕒 与已完成历史弹窗的日期都跟着好读
- chore: dev 端 `/__todo_file`（新增 / 编辑任务）允许空 `date`：填了必须合法，留空则整行不写 `date` 字段（编辑时清空也会去掉该字段）
- 备注：`date` 保留而不删——待办列表排序靠它（`todoItems()` 按 `date || 今天`）

feat(todo-board): 搁置中的任务从「进行中」列移到底部折叠区

- feat: 「进行中」列底部新增搁置区，默认只显示一行「⏸ N 项搁置中 ▾」，点开才铺出卡片（`data-tb-paused-toggle` → `pausedOpen`，带 `aria-expanded`）；卡片本身不变，仍有搁置徽章与「恢复排期」
- feat: 搁置中的任务从待办 / 进行中列彻底下线（`todoItems()` / `doingItems()` 都排除 `pausedAt`），列头计数与顶部统计的「进行中」数量随之扣除——搁置不该再被读成「仍在推进」
- chore: 甘特图工具栏那行「⏸ N 项搁置中未上轴」提示保留（它记录的是「没上轴的有哪些」），与看板折叠区同源（都走 `pausedItems()`）
- chore: 新增 `.todo-paused*` 样式（虚线分隔 + 沙色小按钮），样式块紧随 `.todo-board-pagination`，避免被后文的 `.todo-card` 基础规则反向覆盖
- feat: 搁置卡片去掉「搁置中」徽章（折叠区标题已经说明它是搁置项），搁置起点 / 天数 / 恢复后怎么排全部移到「恢复」按钮的悬停提示；随之删掉已无引用的 `.todo-card-badge-paused`
- feat: 「恢复」改成一键「恢复并顺延」——`resumeShiftDays()` 算出「第一个未完成的阶段距今天几天」，恢复时把这几天整段加到 `plannedStart` / `plannedEnd` 与所有阶段上（`shiftPhases`）：没做完的那段从今天接着排，已完成阶段的相对关系不变。整段平移而非只挪尾巴，是为了不产生校验规则不允许的「阶段空档」；原计划本来就在将来时按钮显示「恢复排期」且不动任何日期

fix(todo-board): 归档记录不再吃到同 id 活动任务的状态补丁（撞号 bug）

- fix: `archivedItems()` 不再整包吃 `todo-state.json` 的状态补丁——活动任务与归档任务共用 id 命名空间，历史上出现过撞号（`l32`：活动「猫猫：合作-1931 商量周边」/ 归档「过马路去校医院看牙齿」；`c10`：活动「聊天站…」/ 归档「猪窝的美食地图…」）。撞号时活动任务的 `plannedStart` / `plannedEnd` / `phases` / `pausedAt` 会串到早已完成的历史条目上：表现为已完成的任务出现在甘特图「N 项搁置中未上轴」提示里，勾选「显示已完成」还会多出一条 9/7—9/20 的横条
- fix: 甘特图的搁置提示只统计活动任务（`pausedItems()` 只看 `allItems()`）——归档条目永远视为已完成，不存在「被搁置」
- refactor: `applyTodoState(item, phases, patch?)` 支持显式传入补丁（空对象 = 这条不许吃状态补丁），供归档条目在撞号时跳过状态合并

feat(todo-board): 新增「搁置」——等待 / 暂停中的任务从甘特图下线，但记录全保留

- feat: 看板卡片新增「搁置」按钮（进行中且有完整排期的任务才显示），点后任务从甘特图轴上下线：排期、阶段、进度点一个都不删，等待期在时间轴上留白（空窗）；卡片上加「⏸ 搁置中 · 自 9/14（第 10 天）」徽章，排期显示为「原计划 9/7—9/20」，主操作由「调整排期」换成「恢复排期」
- feat: 甘特图工具栏（按天 / 按小时两个视图都在）多一行提示：「⏸ N 项搁置中未上轴：标题（自 x/x）—— 在看板卡片上点「恢复排期」即可放回时间轴」，最多列 3 条，其余并成「等 N 项」
- feat: 「恢复排期」只清 `pausedAt`，不重排任何日期——原排期与阶段原样回到轴上；原计划结束日已过期时，toast 提示拖一下横条即可顺延（拖动会带着阶段整体平移）
- chore: 状态补丁新增可选字段 `pausedAt`（YYYY-MM-DD 搁置起始日），dev 同步中间件 `normalizeTodoPatch` 与 `scripts/validate-todo-state.mjs` 同步放行该字段
- 动机：推进中的事（等反馈 / 等别人 / 被别的需求挤掉）停住时，原来只能用「等待反馈和下一轮讨论」这类阶段去占时间轴，等两三周就把甘特图撑得很长；而「移出甘特图」会清空排期与阶段（已完成那几段的记录一起丢）。搁置是介于两者之间的状态：下线、不动数据、随时可恢复
- chore: 卡片顶部一行太挤，徽章文案精简——搁置徽章只留「搁置中」三个字（原「⏸ 搁置中 · 自 9/14（第 10 天）」会折成两行把卡片顶高），悬停提示里给全信息：已搁置几天、搁置开始日（YYYY-MM-DD）、原计划区间、以及「原计划已过期 → 恢复后拖一下横条顺延」
- fix: 卡片顶部徽章不再被动作按钮压窄折行——`.todo-card-badge` 加 `flex: 0 0 auto` + `white-space: nowrap`（原来「🏠 生活」会被挤成「生 / 活」两行），`.todo-card-topline` 允许换行、`.todo-card-actions` 自身可折行右对齐，窄卡片下按钮换行而不是挤压徽章

feat(todo-board): 站点下拉只留站点名，版本信息交给右边的版本框

- chore: 站点下拉的选项由「熊窝（当前 0.32.0）」改成只写站点名「熊窝」——右边版本输入框的占位本来就是「当前 0.32.0」且随站点切换更新（`siteVersionHint`），选项里再带一遍是重复信息，列表也更干净
- fix: 「新增 / 编辑任务」表单关闭浏览器表单历史提示——`<form>` 与版本 / 需求名称 / 链接三个输入框都加 `autocomplete="off"`，不再在版本框下方弹出上次输入过的旧版本号（如选着「熊电台」却提示 `v0.3.0`）

feat(todo-board): 任务命名规范落地到「新增 / 编辑任务」弹窗

- feat: 看板分类选到「编程」时，弹窗标题拆成「站点 + 版本 + 需求名称」三段结构化输入并实时拼出标题预览，保存时按 `{站点} v{版本}：{需求名称}` 写回；生活 / 科研看板保持原来的「任务名称 + 备注」简单表单，切换看板即时切换表单形态（只切显隐、不重建弹窗，已输入内容不丢）
- feat: 站点快捷片（熊窝 / 猪窝 / 猫猫 / 开发笔记 / 常识笔记 / 科研笔记 / 聊天站 / 游戏笔记）一点同时填入站点与下一个里程碑版本（`0.32.0 → v0.33`），光标自动跳到需求名称
- feat: 各站点当前版本在 dev / build 时从兄弟仓库 `package.json` 读取并注入页面（`window.__SITE_VERSIONS`），不用手工维护；线上 Actions 只检出本仓库，读不到就退化成空表，手填即可
- feat: 「站点」字段由文本框改为**下拉菜单**（复用看板分类那套自定义下拉），候选来自构建时注入的 `window.__TODO_SITES`（`index.astro` 的 `SITE_REPOS`，与各仓库 package.json 同源）；选中后版本输入框的占位提示变成「当前 0.32.0」，填哪一版由用户自己定（不再自动写下一里程碑）；编辑老任务时若站点不在清单里，会自动并到候选首位并选中
- chore: 删掉整排站点快捷片——`.todo-new-site-chips` / `.todo-new-site-chip` 样式、`TODO_SITE_CHIPS` 常量、点击与输入同步逻辑全部清理；看板分类与站点两个下拉共用 `closeTodoDropdowns()`（点外部收起、互斥展开）；站点候选约 9 项，列表限高 236px 内部滚动
- feat: 站点清单补上改名的「熊电台」（仓库目录 `bear-radio`，即原 `devnotes-ai-gateway`，当前 `v0.2.0`），下拉候选变为 9 项：熊窝 / 猪窝 / 猫猫 / 熊电台 / 开发笔记 / 常识笔记 / 科研笔记 / 聊天站 / 游戏笔记
- feat: 编程看板的备注栏改为「具体需求」，保存时按一行一条自动编号 ①②③（单行备注当自由文本不编号，已带编号的不重复加），长度仍限制 500 字；生活 / 科研的备注保持原样不编号
- feat: 编辑已有任务时反向解析标题回填三段（兼容「熊电台-v0.1.0~v0.2.0」这类早期写法）；实测 16 条真实标题中 15 条往返完全一致，另一条只把 `-` / ` : ` 归一成规范分隔符
- feat: 「新增任务」弹窗按参考稿重做视觉：奶油底（`#faf7f2`）+ 两块白卡（左「定名字」/ 右「填细节」），表头改「圆形加号徽标 + 深棕粗体标题 + 一句副标题」；输入框统一 10px 圆角 / 白底 / `#ece3d8` 描边，链接框带行内图标，需求名称带橙色必填星号
- feat: 表头右上换成熊插画 `public/assets/todo-board/bear-progress.png`（原图 1080×491 自带透明通道，裁掉四周空白后按 2 倍展示尺寸缩到 340×171、57KB；页面上按 170px 渲染，≤760px 隐藏），插画自带「小步前进，也是进步 ✨」文案，不再另写一行小字
- fix: 表头左右同高对齐——`align-items: stretch` 让 header 高度由 170px 插画决定，左侧「徽标 + 标题」块与右侧「插画 + 关闭按钮」各自在整块高度里垂直居中，上下底边齐平（原来是 `flex-start`，左侧比插画矮就“起飞”了）
- chore: 「新增任务」弹窗去掉右上角 ✕（底部「取消」已承担关闭语义，Esc / 点遮罩同样能关），表头右侧只留熊插画；随之删掉 `.todo-new-header-side` 的 42px 让位内边距与 `.todo-modal-new .todo-modal-close` 定位规则
- fix: 弹窗两栏改为等高——`.todo-new-grid` 用 `align-items: stretch`，网格行高由较高的那张白卡决定，矮的一张自动拉平
- chore: 去掉「站点与版本」下面那行格式说明（信息与站点快捷片、版本占位符重复），并清理已无引用的 `.todo-new-field-note` 样式；站点快捷片整体收小（内边距 `4px 10px`、字号 11.5px、间距 6px），8 个片在左栏排两行即可，不再用三行把卡片顶高
- feat: 标题预览从虚线框改成**底部结果条**（`todo-new-footer`）：左侧 📝 图标 + 「标题预览 / 将根据需求名称自动生成」+ 拼好的标题，右侧「取消 / ＋ 添加任务」，与弹窗底边同圆角；站点快捷片支持选中态（`is-active`），手打站点名也会自动对上
- feat: 弹窗宽度 920px、两栏（`todo-new-grid`），窄屏（≤760px）退回单列且底栏折成上下两段
- fix: `.todo-modal-new` / `.todo-new-header` 改用两级选择器（`.todo-modal.todo-modal-new`、`.todo-modal-header.todo-new-header`），否则会被文件后部的 `.todo-modal` 基础规则反向覆盖（宽度 / 内边距 / 底色全都压不上去）
- feat: 「新增任务」弹窗的默认看板分类从「生活」改为「编程」（原来取的是汇总看板的第一项），站点 / 版本与标题预览随之默认展开；编辑既有任务时仍以该任务原本的看板为准
- chore: `.todo-modal` 增加 `max-height: calc(100vh - 32px)` 与内部滚动，字段变多后矮窗口不会把操作按钮顶出屏幕

## v0.32.0 — 2026-09-19

feat: 首页与仪表盘拆分 —— 首页改生活概览，仪表盘只留想做清单 + 追踪看板

- feat: 新增首页生活概览 [LifeOverview.astro](src/components/LifeOverview.astro)，承载信息密度不高的日常模块：渐变天空、三城天气（点击查看未来 5 天）、可翻月日历、GitHub 主页、网易云歌单；首页画布宽度与 CRM 页面统一为 `min(1400px, 100vw - 20px)`
- feat: 生活仪表盘 [LifeDashboard.astro](src/components/LifeDashboard.astro) 瘦身为「想做清单 + 追踪看板」两块，天气 / 日历 / GitHub / 歌单等模块全部移出
- feat: 新增「想做清单」[WantList.astro](src/components/WantList.astro) + [want-data.ts](src/data/want-data.ts)——无期限、无状态的轻清单，每条只有 `{ text, tag?, added? }`，与任务看板（有排期 / 状态机 / 归档）刻意区分，挂在仪表盘顶部
- refactor: 删除旧首页轨道式画布 `HomeCanvas.astro` 及其全部专属样式，`SiteLayout` 移除 `hideSidebar` / `home-shell` / `home-layout` 分支
- fix: 首页 `.app-shell` 改用独立的 `home-shell` 类，不再复用 `dashboard-shell`，避免两页宽度耦合

## v0.31.0 — 2026-09-15

feat(crm): 关系管理页整体重构与体验收敛

- feat: 活跃度趋势图重做——按容器真实像素设 viewBox（点不再被拉成椭圆）、单调三次平滑曲线、渐变面积填充、Y 轴按数据取整、峰值点高亮；并修复「点开 tooltip → 关闭 → 再也点不开」的 bug（tooltip 改为常驻节点、只切换显隐）
- feat: 长期未联系提示由独立红色块改为「姓名旁状态药丸 + 最近联系行内 chip」，悬浮显示建议文案，不再挤占详情高度
- feat: 手机号 / 邮箱复制按钮真正可用（Clipboard API + `execCommand` 回退，成功后按钮变绿勾 + toast）
- feat: 相关任务勾选归档——确认悬浮窗 → 任务标记已完成并归档、移出列表，跟进提醒同步消失
- feat: 编辑联系人弹窗由右侧抽屉改为居中悬浮窗（暖色卡片 + 径向渐变 + 弹入动画），与互动 / 任务 / 归档弹窗统一
- feat: 删除各大标题与弹窗的中英双语 kicker（PEOPLE IN YOUR CIRCLE、TOUCHPOINT LOG、NEXT ACTIONS 等），只保留中文
- feat: 互动记录固定展示最近 4 条；详情底部卡片改为按内容高度，避免被强行拉高裁切
- feat: 互动弹窗去掉「正在为 XX 记录一次互动」上下文行，标签精简为 类型 / 时间 / 摘要，时间与摘要改为整行堆叠、间距对齐
- feat: 添加任务弹窗去掉上下文行与「今天 / 3天后 / 下周」快捷日期
- feat: 数据看板重构——去掉「RELATIONSHIP DASHBOARDS / 关系数据看板」标题行，第一行 KPI 条、第二行左「待跟进」（加卡片容器）/ 右「互动时间线」；看板宽度与主视图一致、卡片按内容自适应；互动时间线保持 2 条高的滚动容器，「回到联系人」置于卡片右上角
- fix: 侧栏「智能筛选」标题与右侧搜索框顶对齐
- chore: crm.css 缓存版本 v74 → v94

## v0.30.3 — 2026-09-14

chore(todo-board): 甘特图底部只留颜色图例，操作提示并入标题下的副标题

- chore: 「拖动整条移动计划，拖动两端调整日期」「点击横条即可编辑阶段」两句提示从底部图例行挪到 `.todo-gantt-subtitle`（任务甘特图下方那行字），底部图例行只剩「进行中 / 已完成 / 未开始阶段」三个色块
- chore: 按小时视图的图例同样只留色块（顺带补齐之前漏掉的「未开始阶段」）；原图例里的「另有 N 项仅按日期排期」并入小时轴副标题，只在真有纯日期任务时出现
- chore: `renderHourGantt` 不再接 `allScheduledItems`（只为拼图例提示用），提示改由 `renderGantt` 统一算

feat(todo-board): 任务看板默认视图改为甘特图

- feat: `currentView` 初始值由 `'board'` 改为 `'gantt'`——进站第一眼是「当前在推进什么、排到哪一天」，更贴近当天的工作焦点；不记忆上次的视图切换，每次进站都从甘特图开始
- chore: `todo-board/index.astro` 的骨架屏（脚本执行前那一帧）不再摆「待办 / 进行中 / 已完成」三列，改为一句话占位「正在排布甘特图…」，避免默认视图闪一下看板

feat(todo-board): 点击甘特横条直接唤起阶段编辑

- feat: 横条的「点击」与「拖拽」用同一套 pointer 逻辑区分——抬手时排期与按下时完全一致就判定为点击（没有真正拖动），直接打开阶段编辑弹窗；真拖动（含拖出去再拖回原位）仍走原来的写排期逻辑
- feat: 阶段色块新增 `data-phase-id`，点中某一段时弹窗里光标直接落在这一段的名字上（按天 / 按小时视图一致），改完回车就结束这一段编辑
- feat: 点阶段分割线但没拖动，同样按点击处理，打开左侧那一段；弹窗入口统一收敛到 `openPhaseEditor(itemId, phaseId?)`，「分阶段」按钮与横条点击共用
- feat: 键盘可达性——按天横条本身是 `button`，聚焦后回车 / 空格也能打开阶段编辑
- chore: 两个视图的图例补一句「点击横条即可编辑阶段」；打开弹窗前先收掉阶段悬浮窗（其 `z-index: 130` 高于弹窗的 101）

feat(todo-board): 阶段编辑弹窗的阶段名可换行，长名字不再被截成一个「…」

- feat: 阶段名由单行 `<input>` 改为 `<textarea rows="1">`——长阶段名（如「整理一阶段材料和 QA：账目表等、第一轮 QA」）在框内换行显示完整
- chore: 外观完全沿用原来那个输入框（同样的 padding / 边框 / 圆角 / 底色 / 字号，不覆盖行高，单行时与其他字段等高）；**不做拖拽缩放**（`resize: none`），弹窗宽度与阶段行的日期列宽都保持原样
- feat: 高度由 `autosizePhaseTitleField` 按 `scrollHeight + 边框` 实时算出，最多长到 66px（约 3 行），再长才在框内滚动（未到上限用 `overflow: hidden`，避免单行时右侧多出滚动条）；窗口尺寸变化时重新量一次，折行数变了不会被裁掉
- feat: 阶段名是单行语义（要进甘特条标签和悬浮窗），回车不再换行而是结束这一段编辑；中文输入法确认候选词的回车（`isComposing`）不会被抢
- feat: `syncPhaseDraftFromDom` 读取阶段名时把折行 / 连续空白收成单个空格，避免多行输入把换行带进阶段名
- feat: 打开「分阶段」弹窗时自动把光标放到当前阶段（进行中 > 未开始 > 第一段）输入框末尾；点「＋ 添加阶段」后光标直接落在新阶段名上并全选占位名，刷新重建 DOM 也不丢焦点（`phaseFocusPending` → `focusPendingPhaseField`）
- chore: 弹窗顶部去掉「划分任务阶段」kicker（和弹窗内容重复），只留任务名当标题；单行标题行高对齐右侧 28px 关闭按钮

feat(todo-board): 甘特图「按小时」任务支持一键转为按天排期

- feat: 小时轴任务行新增「转为按天」按钮（`data-tb-hour-to-day` → `convertHourlyItemToDay`），丢弃 `plannedStartTime` / `plannedEndTime`，任务回到日期轴；仍受「线上只读」限制，只在本地 dev 显示
- feat: 转换时日期区间沿用原首尾两天；阶段先剥离整点端点，再用 `splitEvenly` 按天平均铺满整段，阶段数多于原天数时把计划结束日顺延到刚好放得下（此时 confirm 说明），避免 `normalizePhases` 按天归一化时把放不下的阶段截断丢失
- feat: 转换成功后自动切回「按天」视图并 toast 展示新的计划区间，避免任务离开小时轴后不知去向
- chore: 小时视图按钮组改为三列（`.todo-gantt-actions.is-triple`，≤768px 退回两列），新增 `.todo-gantt-convert` 暖色样式；小时图例补充「转为按天」提示

## v0.30.2 — 2026-09-04

feat(todo-board): 甘特图新增「按小时」视图，任务排期支持整点时间端点

- feat: 甘特图工具栏新增「按天 / 按小时」切换；按小时视图把计划区间按整点小时铺成行序，适合 1–2 天内的小任务
- feat: 任务排期新增可选 `plannedStartTime` / `plannedEndTime` 整点字段，两端同时存在时任务进入「按小时」视图；阶段弹窗支持填写起止整点时刻
- feat: 小时任务阶段按连续整点归一化，后一阶段从前一阶段结束时刻立即开始，末阶段自动收在结束时刻；按小时拖动整条/分割缝边界保持整点对齐
- feat: `POST /__todo_sync` 落盘前按小时语义归一化阶段（排序、连续铺满、闭合末段），与前端渲染同一套逻辑
- feat: `todo:validate` 与 GitHub Actions 校验整点排期——`HH:00` 格式、起止时刻须成对、结束须晚于开始、小时任务阶段时间端点合法
- feat: 甘特条按小时视图显示各阶段起止时刻，阶段色带按整点切片；看板卡片阶段缩略条保留
- chore: todo-data.ts 的 `TodoItem` 接口与 todo-state.json 支持小时排期字段；同步 l8（带赫兹去博辰复诊，整点排期）、r5（阶段补充）等任务数据

## v0.30.0 — 2026-09-01

feat(todo-board): 看板视图支持「新增任务」弹窗，直接写回 todo-data.ts

- feat: 看板 header 新增「➕ 新增任务」按钮（仅本地 dev 可编辑），弹窗填写看板分类、名称、链接、备注、目标日期五项；新建任务一律先进待办，排期走卡片上的「开始排期」；看板分类为自定义样式的下拉（替代原生 select 系统弹出）
- feat: 新增端点 `POST /__todo_file`：add 往 `src/data/todo-data.ts` 对应看板的 items 数组头部插入一行（id 按看板前缀 l/c/r 取现有最大值递增），remove 按 id 整行移除；字符串统一 JSON.stringify 序列化，引号 / 换行不破坏单行格式；写盘复用 self-write 抑制窗口，客户端同步内存看板数据保持 UI 一致
- feat: 卡片「删除」按钮对全部非归档任务开放（confirm 确认），与弹窗新增走同一条 todo-data.ts 写回链路——任务清单从此只有 todo-data.ts 一个来源
- chore: 上一版 customItems 方案整体退役（`/__todo_sync` 协议字段、渲染合并路径、`todo:validate` 校验全部移除），已有自定义条目手动迁移进 todo-data.ts
- fix: 新任务 id 生成只扫活跃文件，会撞上归档文件里同前缀的更大编号（验收时 l33 与归档 l33 撞号，状态补丁互相污染、弹窗标题张冠李戴）——改为扫活跃 + 归档两个文件取全局最大值递增，冲突条目已改号 l39

## v0.29.0 — 2026-09-01

feat(todo-board): 本地编辑不丢视图 + 分割缝拖拽修复 + 工具栏精简

- fix: 拖拽阶段分割缝时右阶段预览错位重叠——列号原来相对左阶段起点计算，只有第一道缝碰巧正确；改为统一以任务条起点为参照（`leftStartColumn`），并为阶段格子加 `grid-row: 1` 兜底，任何预览值都不会把阶段挤到第二行
- feat: 借鉴 lifenotes/devnotes 本地 CMS 的做法，`astro.config.mjs` 中 todo-sync / tracker-sync 自写盘后 2.5s 内吞掉 dev server 的 `full-reload` / `update` 广播——本地编辑甘特图不再整页刷新、视图不再被重置回看板
- chore: 移除甘特图「划分阶段」按钮的阶段列表悬浮窗与「定位今天」按钮；工具栏改为副标题左对齐、「显示已完成」与计划区间在右侧对齐成一列

feat(todo-board): 甘特条支持阶段划分，长任务拆成有名字的连续子区间

- feat: 任务补丁新增 `phases[]`（id/title/start/end/status），阶段连续无缝铺满计划区间，首阶段对齐 plannedStart、末阶段收在 plannedEnd
- feat: 甘特条从实体色块改为阶段轨道容器，每段独立着色（未开始灰 / 进行中橙 / 完成绿）；≥3 天的阶段显示名称，更短的只留颜色与悬浮提示
- feat: 甘特行新增「分阶段 / 阶段 N」入口，弹窗按「切割点」编辑（每行只填结束日期，下一段自动从次日开始），支持添加、删除、平均分配、清除
- feat: 拖拽整条时阶段整体平移；拖拽两端时只动首/末阶段边界；移出甘特图时清除阶段
- feat: 看板卡片新增阶段缩略条，按天数比例着色并显示当前阶段名
- chore: 阶段编辑用草稿数组驱动，避免 refresh() 重建 DOM 时丢失用户输入
- chore: `/__todo_sync` 支持 phases 字段，落盘前统一归一化（排序、消除空档、末阶段闭合、天数不足时截断多余阶段）
- chore: `todo:validate` 增加阶段校验——id/名称/日期格式、start ≤ end、阶段间不得留空档、末阶段必须收在 plannedEnd
- chore: 拖拽手柄改为绝对定位，不再占用布局流，保证阶段列宽与日期轴严格对齐

feat(todo-board): 任务看板状态去 localStorage 化，真源迁入仓库随发布构建同步

- feat: 新增 `src/data/todo-state.json` 作为任务状态唯一真源（status / plannedStart / plannedEnd / completedAt 字段级补丁，结构与原 localStorage 补丁一致）
- feat: `astro.config.mjs` 新增 `todoSyncPlugin`，本地 dev 下看板操作通过 `POST /__todo_sync` 合并写回仓库状态文件（按任务 id 整体替换，空补丁即清除）
- feat: 构建时状态文件经 `define:vars` 注入页面并内联进静态产物，线上零额外请求；`git push` 后 CI 校验通过即发布，全设备一致
- feat: 线上看板转为只读：不渲染编辑按钮、禁用甘特条拖拽，header 显示「🔒 线上只读」徽标；本地 dev 全功能可编辑，写盘成功才算修改成功
- fix: 归档任务不再可能被本地残留补丁污染状态（原 localStorage 补丁覆盖归档状态的根因消除）
- chore: 新增 `npm run todo:validate` 校验脚本；pre-push hook 与 GitHub Actions 均加入任务状态校验，未提交的状态文件将阻断推送/发布
- chore: 本地 dev 首次打开看板时自动把旧 localStorage（`bear-home.todo-board.v1`）补丁迁移进仓库并清除旧 key

## v0.25.0 — 2026-08-08

feat(routes): 路由表「娱乐」分组更名为「购物娱乐」，新增京东商城入口

- feat: `entertainment` 分组 `title` 由「娱乐」改为「购物娱乐」，`id` 保持不变，分组图标改为 🛍️
- feat: 分组内 Bilibili 之后新增「京东」条目（`https://www.jd.com/`），tags 为 购物/电商
- feat: 新增京东本地图标 `public/assets/routes/京东.png`（favicon 来源，已归一化为 256×256 PNG）
- chore: Astro 从 5.x 升级至 7.2.0，并同步更新 npm 锁文件
- chore: 保持纯静态页面构建，不引入内容集合迁移

## v0.24.1 — 2026-08-05

feat(routes): 路由表图标全面本地化，移除 Google favicon API 依赖

- feat: 为路由表 16 个 URL 条目新增本地图标，统一存于 `public/assets/routes/`（256×256 PNG）
- feat: 图标内容做透明边裁剪并铺满画布，避免不同站点 favicon 显示尺寸不一致
- fix: 移除路由卡片/列表/瀑布流对 `www.google.com/s2/favicons` 的运行时依赖，国内访问不再破图
- fix: 无图标条目回退显示分类 emoji，而非外部 API
- chore: 新增 `scripts/download-route-icons.py` 与 `normalize-route-icons.py` 维护脚本
- chore: 「猫猫」站点域名更新为 `cat.xdubear.cn`

## v0.23.1 — 2026-08-02

fix: 天气预报弹窗与追踪快照一致性

- feat: 点击西安、南宁或威海天气卡片查看未来 5 天预报
- feat: 新增温度、天气类型、降雨概率和最大风速展示，并适配移动端弹窗
- fix: 修复正式站读取追踪快照的路径问题
- fix: 集中维护长期目标配置，自动迁移旧 localStorage / 快照中的目标金额
- chore: 增加追踪快照迁移脚本、推送前校验和 GitHub Actions 校验

## v0.21.3 — 2026-08-02

feat(lol): 英雄速查搜索支持英雄称号匹配

- feat: 英雄数据新增称号字段（如安妮 → 黑暗之女），搜索框输入称号也能定位英雄
- feat: 搜索同时匹配 英雄名 / 称号 / 英文id / 外号 / 拼音
- feat: 英雄详情抽屉头部显示称号
- chore: fetch-lol-all.py 生成数据时保留称号字段

## v0.21.2 — 2026-08-02

fix(routes): 优化路由表移动端的信息密度与视图选择

- fix: 分组卡片在移动端保持双列，缩小卡片、工具栏与瀑布流的留白
- fix: 移动端隐藏横向表格列表视图，并在缩窄窗口时自动回到分组视图

## v0.21.1 — 2026-08-01

fix: 补全 v0.21.0 遗漏的 34 个英雄小写头像（macOS 大小写不敏感文件系统下通配符 add 未命中）

## v0.21.0 — 2026-08-01

feat: 英雄联盟板块重构为 RESG 全英雄数据驱动的英雄速查，新增外号/拼音搜索与筛选排序

- feat: 英雄速查升级为全部 173 位英雄，数据来自 RESG（海克斯大乱斗数据站）公开 API 的静态快照；新增英雄索引、海克斯推荐、外号表三个数据文件与两个生成脚本
- feat: 新增英雄详情抽屉，展示每英雄 RESG 实战海克斯推荐（单/双/三/四 Top5 + 胜率 + 对应出装）
- feat: 英雄画廊支持官方定位筛选（战士/坦克/法师/刺客/辅助/射手）、外号/拼音搜索、默认/胜率/热度排序
- feat: 搜索框放工具栏左侧，筛选与排序收进下拉浮窗，交互更简洁
- feat: 新增生成脚本 `fetch-lol-all.py`（拉英雄/海克斯/头像/胜率）与 `gen-nicknames.py`（外号+拼音）
- style: 英雄卡片紧凑化、按定位配色；全英雄头像本地化（RESG 源，173 张）
- fix: 公式详情视图英雄头像 404（数据路径大小写统一为 RESG 小写）
- fix: 无畏契约页「返回熊窝」链接由 `../` 改为 `../index.html`，修复子路径下返回 404
- docs: README 新增「英雄联盟数据更新」章节与脚本说明

## v0.20.1 — 2026-07-31

feat: 更新首页路由表

- 替换路由表图标
- 替换首页

## v0.17.0 — 2026-07-29

refactor: 会员订阅迁移至猪窝 home

- refactor: 删除「会员订阅」侧栏入口、页面组件与数据源
- docs: 移除熊窝的会员订阅维护说明
- note: 会员订阅现由猪窝 `/membership/` 页面统一承接

## v0.14.0 — 2026-07-25
refactor: 每日看板迁移至猪窝 home 站

- refactor: 移除每日看板 Tab、TodoBoard 组件与 todo-data 数据层
- refactor: 看板功能迁移至 home 站「每日看板」页面，使用 Astro + 原生 JS 重写
- chore: 删除 js/components/TodoBoard.js、js/todo-data.js

## v0.13.0 — 2026-07-21
refactor: 个人开发时间线迁移至 DevNotes，并完善每日看板

- chore: 删除 `.opencode/skills/bear-sync/`，对应工作流（任务/支出 → Obsidian 日记同步）已于 v0.8.0 随日记与支出记录一起迁移至 home 站
- refactor: 删除「个人开发时间线」Tab、Vue 组件、旧数据与样式，功能迁移至 DevNotes「开发时间线」
- refactor: 时间线同步脚本和 hook 模板迁移至 `devnotes/scripts/`

## v0.12.1 — 2026-07-19
fix: 修复未完成待办跨日后消失

- fix: 待办列改为显示 `date <= 今天` 的 `todo`，昨天未完成的任务继续保留
- feat: 跨日待办显示「跨天待办」提示，未来日期任务仍不会提前出现
- docs: 同步每日看板的跨日规则与组件缓存版本

## v0.12.0 — 2026-07-18
feat: 新增英雄联盟攻略与每日看板两大板块

- feat: 「英雄联盟」汇总法师公式 3.0、ADC 公式 4.0、AP 刺客 3.0 三套攻略视图
- feat: 装备与英雄图片全部本地化，攻略数据独立维护且支持桌面、手机响应式布局
- feat: 「每日看板」提供视频、科研、编程、生活四类任务及待办、进行中、已完成三列视图
- feat: 看板支持当天任务、跨天进行中任务与按分类、日期归档的已完成历史
- fix: 补齐 TodoBoard 组件加载，并修正带参数状态过滤器导致的空白页
- docs: 同步站点架构、内容维护方式与版本说明

## v0.11.0 — 2026-07-18
feat: 英雄联盟新增 AP 刺客 3.0 视图

- feat: 攻略切换扩展为「法师公式 3.0 / ADC 公式 4.0 / AP 刺客 3.0」三个标签
- feat: 根据 `BV1snTk65EPf` 转写与画面校对，录入带咒刃统一公式和六名不带咒刃英雄的独立公式
- feat: 快速规则补充守护者法球、术士果汁盒换法穿鞋、自购重伤球和团战切入原则
- feat: 新增 AP 刺客装备与英雄本地图标，页面继续保持零 CDN 依赖
- style: 不带咒刃采用双栏英雄查表卡片，手机端收为单栏

## v0.10.0 — 2026-07-18
feat: 英雄联盟新增 ADC 公式 4.0 双视图

- feat: 英雄联盟板块顶部新增「法师公式 3.0 / ADC 公式 4.0」分段标签
- feat: 根据 `BV1A8j46xEsV` 转录与画面校对，录入两件套公式、11 级海克斯分支、界弓特例和后续按需装备
- feat: 新增 ADC 装备与英雄本地图标，继续保持页面零 CDN 依赖
- style: ADC 使用红橙色流程卡片；桌面三栏、手机单栏响应式布局
- fix: ADC 手机标题固定为两行，避免窄屏随机断字

## v0.9.0 — 2026-07-18
feat: 新增「英雄联盟」出装公式板块

- feat: 侧边栏新增「英雄联盟」Tab，采用独立深蓝黑攻略主题
- feat: 录入双烧流、爆炸流、开团流、快速规则与瑞兹特例
- feat: 新增 `js/lol-data.js` 数据文件，攻略内容与渲染组件分离
- feat: 装备图标与英雄头像全部保存到 `assets/lol/`，不依赖 CDN
- style: 适配桌面与手机布局，手机端图标自动换行且无横向溢出

## v0.8.1 — 2026-07-13
feat: 路由表新增「常用链接」分类，会员订阅补充 ChatGPT Plus，修复备注列截断

- feat: 路由表 `routeCategories` 新增「🔗 常用链接」分类（LDXP 神秘小铺 / OpenTheRank / OpenRouter Rankings），置于「个人站点」之后
- feat: 会员订阅 `membershipRecords` 新增 ChatGPT Plus（到期 2026-08-12，¥25/月，备注登录账号）
- fix: 会员表 `.mt-note` 改为换行显示，备注不再被单行省略号截断

## v0.8.0 — 2026-07-11
refactor: 移除「日历」「记账」两个 Tab 及构建管线（大版本）

- refactor: 删除 CalendarView / ExpenseView 组件及注册
- refactor: 删除 calendar / expense 两个 Tab、expenseCategories / manualRecords / recordLabel / dailyRecords
- chore: 删除 js/diary-data.js、js/expense-data.js、scripts/* 构建管线与 pre-commit hook
- docs: README 同步说明每日追踪与支出记录已迁移至 home
- 注：home 新增「每日追踪」「支出记录」两个 Tab 承接全部功能（见 home v0.9.0）
