{
  "name": "bear-home",
  "private": true,
  "version": "0.30.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 4321",
    "build": "astro build",
    "preview": "astro preview",
    "install-hooks": "sh scripts/install-hooks.sh",
    "tracker:migrate": "node scripts/migrate-tracker-snapshot.mjs",
    "tracker:validate": "node scripts/validate-tracker-snapshot.mjs",
    "todo:validate": "node scripts/validate-todo-state.mjs"
  },
  "devDependencies": {
    "astro": "^7.2.0"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.2"
  }
}
